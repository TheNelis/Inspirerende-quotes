{
    "buildCommand": "composer install --no-dev --prefer-dist --optimize-autoloader --no-interaction && php artisan config:cache && php artisan route:cache && php artisan view:cache",
    "outputDirectory": "public",
    "framework": "laravel",
    "devCommand": "php artisan serve"
}